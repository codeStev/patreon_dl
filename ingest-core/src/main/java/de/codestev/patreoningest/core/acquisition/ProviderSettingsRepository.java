package de.codestev.patreoningest.core.acquisition;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ProviderSettingsRepository extends JpaRepository<ProviderSettings, String> {
}
