# Keeps existing end-to-end tests deterministic - background polling would
# otherwise race against a test's own explicit pollMailboxUseCase.poll() calls.
patreon.ingestion.polling.enabled=false
patreon.fulfillment.queue.enabled=false
patreon.fulfillment.folder-sync.enabled=false
patreon.acquisition.claim-queue.enabled=false
